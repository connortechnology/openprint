<? echo (
	$r->content_type('text/javascript');
	my @options = openprint::Location->dropdown( 'type' => $param{'type'}, 'order'=>'lower(name)', 'parent_id'=>$param{'parent_id'} );
	my $json = JSON->new;
	return "
var ddm = \$('$param{child_element}');
var selected = ddm.getValue();
fill_ddm_from_array( ddm, " . $json->encode( @options ) . " );	
ddm_select_by_value( ddm, selected, 0 );
";
) ?>
