<? if ( $$variable{'changed'} ) ?>
<? echo ( 
	$r->content_type('text/javascript');
	my $html = '';
	foreach my $id ( @{$$variable{'changed'}} ) {
		$html .= sprintf(q`new Ajax.Updater( '%1$s', '_ul.html', { method: 'get', parameters: { id: '%1$s' } } );`, $id );
	} # end foreach id
	return $html;
) ?>
<? endif ( $$variable{'changed'} ) ?>
