<? if ( $$variable{'changed'} ) ?>
<? echo ( 
	$r->content_type('text/javascript');
	my $html = '';
	foreach my $id ( sets::union( @{$$variable{'changed'}} ) ) {
		$html .= sprintf(q`new Ajax.Updater( '%1$s', '_ul.html', { method: 'get', parameters: { id: '%1$s' }, evalScripts: true } );`, $id );
	} # end foreach id
	return $html;
) ?>
<? endif ( $$variable{'changed'} ) ?>
