<? if ( $$variable{'changed'} ) ?>
<? echo ( 
	my @drops = sets::union( @{$$variable{'changed'}} );
$log->debug("Drops: @drops");
	$r->content_type('text/javascript');
	my $html = '';
	$html .= "alert('$variable{error}');" if $variable{'error'};
	$html .= "Dialog.alert(\"$variable{alert}\", {width:500, height:100, okLabel: 'close', ok:function(win) {return true;}});" if $variable{'alert'};
	#$html .= "alert('$variable{alert}');" if $variable{'alert'};
	foreach my $id (sort @drops) {
		$html .= sprintf(q`new Ajax.Updater( '%1$s', '_ul.html', { method: 'get', parameters: { ul_id: '%1$s' }, evalScripts:true } );`, $id );
	} # end foreach id
	return $html;
) ?>
<? endif ( $$variable{'changed'} ) ?>
